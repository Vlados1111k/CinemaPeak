# Звіт за ітерацію №3 (Lab 36)

## Quality Gate
* **Загальна кількість тестів**: 32 (24 юніт-тести, 8 інтеграційних тестів).
* **Статус**: Всі тести пройшли успішно.
* **Fault Handling**: Реалізовано стійкість до помилок I/O та пошкоджених файлів конфігурації у `JsonDataStore`.

## Усунені Code Smells та Рефакторинг:
1. **Приховані залежності**: Логіка створення файлів у тестах більше не прив'язана до статичних шляхів. Використовуються ізольовані тимчасові файли ОС.
2. **Поліморфна десеріалізація**: Додано підтримку `JsonDerivedType` для базового класу `Ticket`, що вирішило проблему зчитування абстрактних типів із JSON.
3. **Constructor Binding**: Узгоджено назви параметрів конструкторів із властивостями моделей для коректної роботи `System.Text.Json`.

## Залишкові ризики перед Lab 37:
* Консольне меню (`Program.cs`) досі обробляє ввід через базові конструкції `switch/case`. В наступній лабі планується рефакторинг інтерфейсу користувача.